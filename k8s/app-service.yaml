apiVersion: v1
kind: Service
metadata:
  name: cloud-app
  namespace: cloud-app
  labels:
    app: cloud-app
spec:
  type: LoadBalancer
  selector:
    app: cloud-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
